<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyRadioButtonStyle" parent="android:Widget.Material.CompoundButton.RadioButton">
        <item name="android:textColor">@color/Blanco</item>
        <item name="buttonTint">@color/azulFondo</item>
    </style>

    <style name="btnOk" parent="android:Widget.Material">
        <item name="android:textColor">@color/Blanco</item>
        <item name="android:textSize">15sp</item>
        <item name="android:background">@color/azulFondo</item>
    </style>

</resources>
